package pieces;

/**
 * Coordinate relative to the board
 * 
 * @author Reggie Senal
 * 
 */
public class Position {

	private int x;
	private int y;
	public Position() {
		setPosition(0, 0);
	}
	
	public Position(int x, int y) throws InvalidTilePosition{
		setPosition(x, y);
	}
	
	public void setPosition(int x, int y) throws InvalidTilePosition{
		this.x = x;
		this.y = y;
	}
}
